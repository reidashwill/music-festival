<h4>Stage Name: <%= @stage.name %></h4>

<h4>Artists performing on this stage:</h4>

  <% if @stage.artists %>
    <ul>
      <% @stage.artists.each do |artist| %>
        <li><a href="/stages/<%= @stage.id %>/artists/<%= artist.id %>"><%= artist.name %></a></li>
      <% end %>
    </ul>
    <% else %>
      </p> There are no artists performing on this stage yet!</p>
    <% end %>

    <h4>Add an artist:</h4>

    <form action="/stages/<%= @stage.id %>/artists" method="post">
      <div class="form-group">
        <input id="artist_name" name="artist_name" class="form-control" type="text" placeholder="Artist Name">
      </div>
      <button type="submit" class="btn-danger">Add Artist</button>
    </form>


  <p><a href="/stages/<%= @stage.id %>/edit">Edit Stage</a></p>
  <a href="/festival">Return to festival main page.</a>