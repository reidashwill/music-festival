<h1>Welcome to the music festival</h1>
<h3>Check out the stages below!  Click on them to see the lineup for each stage</h3>

<% if @stages.any? %>

  <ul>
    <% @stages.each do |stage| %>
      <li><a href="/stages/<%= stage.id %>"><%= stage.name %></a></li>
    <% end %>
  </ul>
<% else %>
<p> There are no stages built yet!</p>
<% end %>

<a href="/stage/new">Add a new stage</a>